{
  "name": "simple-chat-by-react",
  "version": "1.0.0",
  "description": "this is simple chat page.",
  "main": "index.js",
  "scripts": {
    "start": "node ./server/bin/www",
    "dev": "webpack --config webpack-dev-config.js -d",
    "prod": "webpack --config webpack-prod-config.js -p",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "co": "^4.6.0"
  }
}
